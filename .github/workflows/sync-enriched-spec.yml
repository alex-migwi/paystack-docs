name: Sync Enriched Paystack Spec

on:
  repository_dispatch:
    types: [enriched-spec-updated]
  workflow_dispatch:
    inputs:
      spec_url:
        description: 'Custom URL to paystack-enriched.json'
        type: string
        required: false

permissions:
  contents: write

jobs:
  sync-enriched-spec:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout paystack-docs repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Download & Update Local Enriched Spec Copy
        if: ${{ github.event_name == 'repository_dispatch' || github.event.inputs.spec_url }}
        env:
          SPEC_SHA: "${{ github.event.client_payload.sha }}"
          ENRICHED_SPEC_URL: "${{ github.event.client_payload.sha && 'https://raw.githubusercontent.com/alex-migwi/paystack-spec-enriched/${SPEC_SHA}/dist/paystack-enriched.json' || github.event.inputs.spec_url }}"
        run: |
          mkdir -p spec
          echo "🌐 GitHub Action fetching spec from remote release: $ENRICHED_SPEC_URL"
          curl -sSL "$ENRICHED_SPEC_URL" -o spec/paystack-enriched.json
          echo "✅ Local spec copy updated: spec/paystack-enriched.json"


      - name: Compile Spec & Track Operational Changes
        run: node scripts/compile-openapi.js

      - name: Commit & Push Updated Local Spec & Generated SDKs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(spec): sync latest enriched spec & update operational changelog"
          file_pattern: "public/* packages/*"