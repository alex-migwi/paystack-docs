import { apiExplorer } from './tags/api-explorer';
import { endpoint } from './tags/endpoint';
import { callout } from './tags/callout';
import { param } from './tags/param';

export default {
  tags: {
    apiExplorer,
    apiexplorer: apiExplorer,
    'api-explorer': apiExplorer,
    api_explorer: apiExplorer,
    endpoint,
    callout,
    param,
  },
};
